<!--
  Template for /codeprobe audit output.
  The orchestrator reads this template and fills {placeholders} with actual data.
  Render as markdown in the user's terminal.

  Placeholders:
    {project_name}       — Name of the project or directory being reviewed
    {date}               — Date of the audit (YYYY-MM-DD)
    {detected_stacks}    — Comma-separated list of detected technology stacks
    {overall_score}      — Weighted overall score (0-100)
    {score}              — Per-category score (0-100)
    {n}                  — Count of findings at that severity level
    {id}                 — Finding ID (e.g., SOLID-001, SEC-003)
    {problem}            — One-sentence description of the issue
    {file}               — File path relative to project root
    {lines}              — Line range (e.g., 45-67)
    {evidence}           — Quoted code or concrete proof from the codebase
    {suggestion}         — What to do to fix it
    {fix_prompt}         — Copy-pasteable prompt for Claude Code to apply the fix
-->

# Code Review Audit Report

## Project: {project_name}
## Date: {date}
## Detected Stack: {detected_stacks}

---

### Overall Score: {overall_score} / 100

### Score Breakdown

| Category | Score | 🔴 Critical | 🟠 Major | 🟡 Minor | 🔵 Suggestion |
|----------|-------|-------------|----------|----------|---------------|
| Security | {score} | {n} | {n} | {n} | {n} |
| SOLID Compliance | {score} | {n} | {n} | {n} | {n} |
| Architecture | {score} | {n} | {n} | {n} | {n} |
| Error Handling | {score} | {n} | {n} | {n} | {n} |
| Performance | {score} | {n} | {n} | {n} | {n} |
| Test Quality | {score} | {n} | {n} | {n} | {n} |
| Code Smells | {score} | {n} | {n} | {n} | {n} |
| Design Patterns | {score} | {n} | {n} | {n} | {n} |
| Framework Usage | {score} | {n} | {n} | {n} | {n} |

---

### 🔴 Critical Issues (Fix Immediately)

{For each critical finding:}

**{id}** -- {problem}
- **File:** `{file}` (lines {lines})
- **Evidence:** {evidence}
- **Suggestion:** {suggestion}
- **Fix Prompt:** `{fix_prompt}`

---

### 🟠 Major Issues (Fix This Sprint)

{For each major finding:}

**{id}** -- {problem}
- **File:** `{file}` (lines {lines})
- **Evidence:** {evidence}
- **Suggestion:** {suggestion}
- **Fix Prompt:** `{fix_prompt}`

---

### 🟡 Minor Issues (Tech Debt Backlog)

{For each minor finding, one line per issue:}
- **{id}** `{file}:{lines}` -- {problem} -> {suggestion}

---

### 🔵 Suggestions

{For each suggestion, one line per issue:}
- **{id}** `{file}:{lines}` -- {problem} -> {suggestion}

---

### Top 5 Quick Wins

{Highest impact, lowest effort fixes -- selected from all findings:}

1. {fix_prompt} -- {why this is a quick win}
2. {fix_prompt} -- {why this is a quick win}
3. {fix_prompt} -- {why this is a quick win}
4. {fix_prompt} -- {why this is a quick win}
5. {fix_prompt} -- {why this is a quick win}

---

### Refactoring Roadmap

{Ordered sequence of changes, considering dependencies between fixes:}

1. **First:** {fix} -- because {reason/dependency}
2. **Then:** {fix} -- depends on step 1
3. **Then:** {fix} -- depends on step 2
4. ...

---

### Fix Prompts (Copy into Claude Code)

{Numbered list of ALL fix prompts, ordered by severity (critical first):}

1. {fix_prompt}
2. {fix_prompt}
3. ...

---

*Generated by code-review-claude v2.0.0 -- Read-only analysis -- no files were modified*
